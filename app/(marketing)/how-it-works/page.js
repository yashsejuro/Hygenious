import HowItWorksClient from './HowItWorksClient';

export const metadata = {
    title: 'How It Works - Hygenious AI',
    description: 'See how Hygenious uses AI to transform your smartphone into a hygiene auditor in 4 simple steps: Capture, Upload, Analyze, and Improve.',
};

export default function HowItWorksPage() {
    return <HowItWorksClient />;
}
